# Full repo deployment reference

These files are the older/self-contained deployment reference for deploying this repository as an agent image.

They are kept for internal validation and historical comparison. The BYO Pi agent product path is now the skill-managed adapter bundle under `.agents/skills/pi-foundry/assets/adapter/` plus the versioned runtime image built from `Dockerfile.runtime`.
