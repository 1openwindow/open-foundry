# Full repo deployment reference

These files are the older/self-contained deployment reference for deploying this repository as an agent image.

They are kept for internal validation and historical comparison. The BYO Pi agent product path is the thin azd adapter under `templates/azd-native/` plus the versioned runtime image built from `Dockerfile.runtime`.
